{
  "routes": [
    {
      "method": "GET",
      "path": "/photo-blogs",
      "handler": "photo-blog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photo-blogs/count",
      "handler": "photo-blog.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photo-blogs/:id",
      "handler": "photo-blog.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/photo-blogs",
      "handler": "photo-blog.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/photo-blogs/:id",
      "handler": "photo-blog.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/photo-blogs/:id",
      "handler": "photo-blog.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
